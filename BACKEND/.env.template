# ══════════════════════════════════════════════════════════════
# AutoPulse – Backend ENV (completar con datos reales)
# ══════════════════════════════════════════════════════════════

APP_URL=http://localhost:3000
CORS_URL=http://localhost:8080

# ── Opción A: Conexión directa (sin pooler) ────────────────────
# Usá esta si tenés la password de la DB de Supabase
# DATABASE_URL=postgresql://postgres:[DB_PASSWORD]@db.dwgyvocxsstcnilyslam.supabase.co:5432/postgres

# ── Opción B: Transaction Pooler (recomendado para Railway) ───
# USER del pooler = postgres.[project_ref]
DATABASE_URL=postgresql://postgres.dwgyvocxsstcnilyslam:[DB_PASSWORD]@aws-0-sa-east-1.pooler.supabase.com:6543/postgres

DB_SSL=true

JWT_SECRET_PASSWORD=autopulse_jwt_2026_dev_local_secreto_largo

# Dejalo vacío para usar análisis simulado hasta tener la key
GEMINI_API_KEY=
