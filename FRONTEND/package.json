{
  "name": "autopulse-frontend",
  "version": "1.0.0",
  "description": "AutoPulse Frontend",
  "scripts": {
    "start": "npx serve . -l $PORT --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}
